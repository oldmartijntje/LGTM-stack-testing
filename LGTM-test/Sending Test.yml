info:
  name: Sending Test
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3001/send
  body:
    type: json
    data: |-
      {
        "message": "your message content"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
